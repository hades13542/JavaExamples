package main;

public class Java18Examples {
    /**
     * Example doc
     * {@snippet :
     * if (whatever.isPresent()) {
     *     System.out.println(whatever);
     * }
     *     }
     * @param args
     */
    public static void main(String[] args) {
        // UTF8 by default, yay
        //./jwebserver ?
        // Java wymusza wszystkie mozliwe kombinacje w switchu (bo sie nie skompiluje), or default :)
        // no i deprecated .finalize() bo to niebezpieczne, uzywajmy try-with-resources
    }
}
